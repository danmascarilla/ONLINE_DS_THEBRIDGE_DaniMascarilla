git --version --> verificar si esta instalado y muestra version

git config --global user."x" "y"

![[git.png]]

git clone "http://..."

![[git2.png]]

git pull --> cada vez que hagamos algun cambio tenemos que hacer git pull. dentro del directorio donde hemos hecho el git clone

git add --> decir a git que considere el archivo una version válida para poder subirlo

git commit --> verifica que el archivo es correcto y lo prepara para subirlo a github

git push --> sube el archivo al repo online

git pull --> actualizar carpeta y conecta con el repo online

