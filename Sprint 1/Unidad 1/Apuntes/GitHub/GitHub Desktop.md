File --> Clone repository --> URL

Cuando haces un cambio en el Github, por ejemplo, crear un archivo llamado test.txt, en el desktop aparece esto:

![[git_desktop.png]]

El fetch origin detecta que hay un nuevo archivo en la web y quiere hacer el pull para que esté en el repositorio del ordenador.
